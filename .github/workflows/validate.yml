name: Validate

on: [push]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate cross-references
        run: pwsh -File .ai/scripts/validate_refs.ps1
        shell: pwsh
